spring:
  application:
    name: day1

  output:
    ansi:
      enabled: always

  profiles:
    active: ${APP_PROFILE:dev}


  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect


  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    clean-disabled: false
    clean-on-validation-error: true

#  mail:
#    host: smtp.gmail.com
#    port: 587
#    username: ${MAIL_USERNAME:demorandomotp@gmail.com}
#    password: ${MAIL_PASSWORD:wfqz dahf nsla qlwp}
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: true
#          connectiontimeout: 5000
#          timeout: 5000
#          writetimeout: 5000

server:
  port: 8080


app:
  jwt:
    issuer: yoedu-ecommerce
    secret: ${JWT_SECRET:yoedu-demo-secret-key-12345678901234567890123456789012}
    access-token-ttl-minutes: 480
    refresh-token-ttl-days: 7


#springdoc:
#  api-docs:
#    enabled: true
#  swagger-ui:
#    enabled: true
#    path: /swagger-ui.html
#    disable-swagger-default-url: true
#    operations-sorter: alpha
#    tags-sorter: alpha